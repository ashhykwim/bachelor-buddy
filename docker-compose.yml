services:
  web:
    build: .
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      TURSO_DATABASE_URL: file:/app/data/bachelor-buddy.sqlite
      PORT: 3000
      HOSTNAME: 0.0.0.0
    volumes:
      - bachelor_data:/app/data

volumes:
  bachelor_data:
